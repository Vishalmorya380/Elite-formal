<!DOCTYPE html>
<html>
<head>
  <title>Admin Forgot Password</title>
  <link rel="stylesheet" type="text/css" href="/css/style.css">
  <link rel="stylesheet" href="/css/authform.css">
  <script src="/js/passwordToggle.js"></script>
</head>
<body>
  <%- include('partials/header') %> <!-- Updated include path -->
  <div class="auth-form">
    <h2>Admin Forgot Password</h2>
    <% if (error) { %>
      <p class="error"><%= error %></p>
    <% } %>
    <form action="/admin/forgot-password" method="POST">
      <div class="form-group">
        <label for="adminCode">Admin Code:</label>
        <input type="text" id="adminCode" name="adminCode" required>
      </div>
      <div class="form-group">
        <label for="newPassword">New Password:</label>
        <input type="password" id="newPassword" name="newPassword" required pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z])(?=.*\W).{10,}">
        <input type="checkbox" onclick="togglePasswordVisibility('Password')"> Show Password
        <p class="disclaimer">Password must be at least 10 characters long and contain at least one uppercase letter, one lowercase letter, one number, and one special character.</p>
      </div>
      <div class="form-group">
        <label for="confirmPassword">Confirm Password:</label>
        <input type="password" id="confirmPassword" name="confirmPassword" required pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z])(?=.*\W).{10,}">
        <input type="checkbox" onclick="togglePasswordVisibility('confirmPassword')"> Show Password
      </div>
      <button type="submit" class="btn">Reset Password</button>
    </form>
  </div>
  <%- include('partials/footer') %> <!-- Updated include path -->
</body>
</html>
